{
  "name": "@cvh/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code Voice Harness desktop app (Electron + React). Deps added at step 10.",
  "scripts": {
    "start": "echo \"desktop app scaffolded at step 10\" && exit 0"
  }
}
